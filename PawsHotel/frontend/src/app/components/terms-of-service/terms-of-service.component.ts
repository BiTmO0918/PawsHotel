import { Component } from '@angular/core';

@Component({
  selector: 'app-terms-of-service', // Selector for the component
  standalone: true, // This component is standalone
  imports: [], // No external modules are imported
  templateUrl: './terms-of-service.component.html', // HTML template for this component
  styleUrl: './terms-of-service.component.css' // CSS file for this component
})
export class TermsOfServiceComponent {
  // Currently, there are no properties or methods in this component.
}
